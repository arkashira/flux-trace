# Flux Trace
A simple metrics collector for monitoring transaction volume and latency.

## Usage
1. Create a `FluxTrace` object.
2. Add transactions using the `add_transaction` method.
3. Scrape metrics using the `scrape_metrics` method.
4. Check for alerts using the `check_alert` method.

## Testing
Run `pytest` to execute the tests.
